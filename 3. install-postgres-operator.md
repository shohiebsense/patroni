user@MacBookPro patroni % helm install postgres-operator postgres-operator-charts/postgres-operator \
  -n postgres-operator
NAME: postgres-operator
LAST DEPLOYED: Tue Feb 10 12:55:51 2026
NAMESPACE: postgres-operator
STATUS: deployed
REVISION: 1
TEST SUITE: None
NOTES:
To verify that postgres-operator has started, run:

  kubectl --namespace=postgres-operator get pods -l "app.kubernetes.io/name=postgres-operator"
user@MacBookPro patroni % 